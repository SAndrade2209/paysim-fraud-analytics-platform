name: 'fraud_detection_dbt'
version: '1.0.0'
profile: 'paysim_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fraud_detection_dbt:
    staging:
      +materialized: view
      +schema: STAGING
    dimensions:
      +materialized: table
      +schema: TRUSTED
    facts:
      +materialized: table
      +schema: TRUSTED
snapshots:
    fraud_detection_dbt:
        dimensions:
          +schema: SNAPSHOTS